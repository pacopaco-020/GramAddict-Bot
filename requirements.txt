# Instagram Version Finder Requirements
# Python dependencies for the Instagram APK analysis toolkit

# Core dependencies (usually included with Python 3.7+)
# No external dependencies required - uses only standard library

# Optional dependencies for enhanced functionality
# Uncomment if you want additional features:

# For better XML parsing (if apktool extraction needs it)
# lxml>=4.6.0

# For progress bars during long operations
# tqdm>=4.60.0

# For better error handling and logging
# colorama>=0.4.4

# For parallel processing optimization
# psutil>=5.8.0